// assignment with a returned value
let changed = 0;

function changed(num) {
    return (num+5)/3;

}

changed = change(10);

let processed = 0;

function processedArg(num) {
    return (num + 3) / 5;
}

processed = processedArg();
